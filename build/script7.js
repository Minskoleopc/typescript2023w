"use strict";
// // program 1
// interface Person {
//     firstName:string ,
//     lastName:string ,
//     age:number
// }
let calculator = {
    add: (x, y) => {
        return x + y;
    },
    sub: (x, y) => {
        return x - y;
    }
};
calculator.add(12, 4);
calculator.add(12, 3);
let SBI = {
    save(rate) {
        return 10;
    },
    loan(rate) {
        return 10;
    }
};
